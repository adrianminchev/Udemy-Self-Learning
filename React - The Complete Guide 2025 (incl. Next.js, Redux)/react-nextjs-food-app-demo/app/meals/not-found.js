export default function NotFound(params) {
  return (
    <main className="not-found">
      <h1>Meal Not Found</h1>
      <p>
        Unfortunately, the specified meal related page resource was not found.
      </p>
    </main>
  );
}
